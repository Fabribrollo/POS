// Placeholder: hoy el frontend lee el puerto del backend desde el query
// string (?apiPort=...) que inyecta main.ts. Si más adelante el frontend
// necesita invocar funcionalidad nativa (impresora térmica, lector de DNI),
// se expone acá vía contextBridge en vez de habilitar nodeIntegration.
